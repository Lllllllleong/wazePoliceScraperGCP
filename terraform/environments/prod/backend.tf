# Remote state backend configuration
# This stores Terraform state in Google Cloud Storage for team collaboration
# and state locking (automatic with GCS backend)
# The backend block is defined in versions.tf to avoid duplicate terraform blocks
